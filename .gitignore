# === OS (macOS) ===
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# === Editor / IDE ===
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# === Security ===
.env
.env.local
.env.*.local
*.pem
*.key
id_rsa
id_rsa.*
*.p12
secrets.json

# === Logs (note: bare "logs/" は使わない — .docs/project-os/logs/ を誤無視するため) ===
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Node.js / pnpm ===
node_modules/
.pnpm-store/
.npm

# === Vite / Build ===
dist/
dist-ssr/
build/
*.local
.vite/

# === TypeScript ===
*.tsbuildinfo

# === Testing (Vitest) ===
coverage/
.nyc_output/

# === Claude Code / AI Tools ===
# settings.json は追跡対象 (Agent Teams flag を共有)。local override のみ無視。
.claude/settings.local.json
# 個人用作業ログ (logging skill 規約)。検証済み知見は /promote-log で shared/ に昇格し追跡。
.docs/logs/local/
