# === OS (macOS) ===
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# === Editor / IDE ===
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# === Security ===
.env
.env.local
.env.*.local
*.pem
*.key
id_rsa
id_rsa.*
*.p12
secrets.json

# === Logs (note: bare "logs/" は使わない — .docs/project-os/logs/ を誤無視するため) ===
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Node.js / pnpm ===
node_modules/
.pnpm-store/
.npm

# === Vite / Build ===
dist/
dist-ssr/
build/
*.local
.vite/

# === TypeScript ===
*.tsbuildinfo

# === Testing (Vitest) ===
coverage/
.nyc_output/

# === Claude Code / AI Tools ===
# settings.json は追跡対象 (Agent Teams flag を共有)。local override のみ無視。
.claude/settings.local.json

# === Harness R&D (public repo には出さない — 個人ハーネスの内部解説/検証 findings) ===
# 計画+観測ログ / ログ全般 / 解説 HTML はローカル保持。公開は project-os (ゲーム設計) のみ。
.docs/plans/
.docs/logs/
.docs/output/
